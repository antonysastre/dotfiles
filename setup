#/bin/bash

for f in `pwd`/copy/*
do
  cp -i $f ~/.`basename $f`
done

for f in `pwd`/link/*
do
  ln -nsi $f ~/.`basename $f`
done

eval 'mkdir -p ~/.vim/autoload ~/.vim/bundle && curl -LSso ~/.vim/autoload/pathogen.vim https://tpo.pe/pathogen.vim'
